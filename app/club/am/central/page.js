import AmCentralView from "@/components/AmCentralView";

export const metadata = {
  title: "Central da Corrida — Amplify Club",
};

export default function AmCentralPage() {
  return <AmCentralView />;
}